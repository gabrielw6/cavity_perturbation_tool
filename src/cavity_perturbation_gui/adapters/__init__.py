"""adapters/ -- the one-entry-point-per-physics-concern boundary between
`cavity_perturbation_gui` and `cavity_perturbation` (docs/gui_module_plan.md
Section 1.2). Plain Python, no Qt import anywhere in this sub-package
(Section 1.4) -- testable with plain pytest.
"""
