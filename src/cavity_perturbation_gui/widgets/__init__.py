"""widgets/ -- PySide6 + PyQtGraph UI, per docs/gui_module_plan.md Section 5.
Every physics call goes through adapters/ (via workers/solve_worker.py) --
no widget constructs a CavityMode, Sample, or solver object itself."""
